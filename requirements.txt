libclang>=14.0
requests>=2.28.0
python-docx>=1.0
tiktoken>=0.5.0  # optional: enables exact token counting (char fallback if missing)
Pillow>=10.0     # flowchart PNG slicing for tall flowcharts (views/flowcharts.py)
numpy>=1.24      # whitespace-band scan for the slicer
fastapi>=0.110   # backend HTTP API
uvicorn>=0.27    # ASGI server for backend/main.py
pydantic>=2.0    # request/response models (transitive via FastAPI, pinned for clarity)
